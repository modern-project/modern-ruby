TODO: investigate the `oj` gem as a JSON replacement
      Supposedly, if you use Ruby 2.4 and up, `oj` is a drop-in replacement for
      the stock JSON gem. We already have it pulled in from a dependency, so
      it's free.